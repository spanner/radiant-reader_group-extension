- if admin?

  - groups = Group.find(:all)
  - if @group && @message.new_record?
    - @message.group = @group

  - fields_for @message do |f|

    %p.group
      = f.label :group_id, "Assign message to group?"
      %br
      = f.select :group_id, groups.map { |g| [g.name, g.id] }, {:include_blank => true}, {:class => 'standard'}
      %br
      %span.formnote Choose a group here and this message will only be sendable to members of that group. Leave blank for general use.
